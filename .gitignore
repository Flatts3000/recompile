# Build artifacts
build/
.gradle/
out/
bin/
*.class
*.jar
!gradle/wrapper/gradle-wrapper.jar

# IDE
.idea/
*.iml
.vscode/
.eclipse/
*.launch
.settings/
.project
.classpath

# NeoForge dev runs
run/
runs/

# OS
.DS_Store
Thumbs.db
desktop.ini
*.swp
*.swo

# Logs
*.log
logs/
hs_err_*.log

# Python tooling caches (scripts/)
__pycache__/
*.pyc
.ruff_cache/

# Local environment / secrets
.env
.env.local
*.local.properties

# Generated docs (if any tooling auto-generates)
docs/CODEMAPS/

# Texture candidate scratch space (raw 1024s, previews, prompt metadata,
# generator scripts, comparison HTML). Only the finalized 16px game textures
# under assets/ are committed - no raw AI output lands in the repo.
gen/
# Selected raw 1024px AI sources (promote copies here to allow re-finalizing
# without re-hitting the API). Kept local; never committed.
art_src/
build_errors.txt
.claude/
